spring:
  application:
    name: wms-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: e0e2a3db-d357-445a-b10e-baafa09ae005
        group: dev
        file-extension: yml  # 默认找服务名.properties文件